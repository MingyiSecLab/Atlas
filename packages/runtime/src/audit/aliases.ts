/**
 * Desktop 侧命名别名（对齐 RuntimePentestEvent / RuntimePentestEngagementSnapshot
 * 的既有命名习惯）。
 */
export type {
  AuditEvent as RuntimeAuditEvent,
  AuditRunSnapshot as RuntimeAuditRunSnapshot
} from './types.js'
